/// A part of the lesson screen's page an introduction can point at, as a
/// `TourTarget.id`. The bar's own parts are `AppHeaderPart`.
enum LessonTourPart {

  /// The whole `OptionalStepBanner` row: badge, question mark and Skip.
  deepDive,
}
